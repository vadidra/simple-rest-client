class Student
  @id
  @first_name
  @last_name
  @course

  def initalize(id,first_name,last_name,course)
    @id = id
    @first_name = first_name
    @last_name = last_name
    @course = course
  end
end